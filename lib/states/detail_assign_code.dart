import 'package:flutter/material.dart';

class DetailAssignCode extends StatelessWidget {
  const DetailAssignCode({super.key, this.assignCode});

  final String? assignCode;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}


  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar());
      
  }

